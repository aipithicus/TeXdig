# Changesets

Version bumps and changelog entries are managed with [changesets](https://github.com/changesets/changesets).

- `pnpm changeset` — record a change (package, bump type, summary) in this directory.
- `pnpm version-packages` — apply pending changesets to package versions and changelogs.
- `pnpm release` — build and publish.
