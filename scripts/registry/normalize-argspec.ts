/** Harvest and document definitions share the engine's checked argspec normalization. */
export { normalizeArgumentPattern as normalizeArgspec } from "../../packages/texdig/dist/binding/pattern.js";
