# Parent oracle inventory

`manifest.ts` inventories all 82 test-side files in the authoritative parent snapshot `20260831_115026`: five latex-utensils test/helper files and 77 unified-latex test/benchmark files. Every record includes the producer revision, original source path, MIT license, producer lineage, the shared `parent-regression-correlated` label, and the exact snapshot shard/content span.

The manifest contains provenance only; it does not vendor or execute parent code. Parent agreement is correlated evidence because the parsers share grammar ancestry and the cases are producer-shaped. TeXdig records each parent independently and joins those observations with its own mechanics laws and gauntlet-derived properties.

The source snapshot is `D:/aipithicus/aipithicus-issues/TeXdig/archaelogy-snapshots/unified-latex-utensils/.snapshot/20260831_115026/unified-latex-utensils_tree.md`. The producer revision fields are latex-utensils `641a665b298260bd0f5a67ce5e7ec6d233b44043` and unified-latex `3c1350edbc8f13ccfbbd5d96919391fefa4d268d`; snapshot row coordinates, rather than the live clone state, remain the content authority.

`control-cases.ts` records the executable start-rule, strict-mode, and timeout controls and cites three independently inventoried parser-test sources. `tests/parent-oracle-manifest.test.ts` first verifies those IDs against the manifest, then executes the controls against TeXdig. The cases are correlated parent-shaped evidence, not an instruction to turn agreement into an expected TeXdig result. Later ports can append observations to the same stable oracle IDs.
