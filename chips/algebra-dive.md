You are executing Chip 01 for the TeXdig project. Read the chip brief first and follow it exactly:
D:\aipithicus\aipithicus-issues\TeXdig\briefs\01-algebra-read.md
Canon to read before starting (for the "why", not to restate): D:\aipithicus\aipithicus-issues\TeXdig\planning\substrate-design.md (the Core read this continues; its §5 is ruled as decision D26), D:\aipithicus\aipithicus-issues\TeXdig\planning\decisions.md (D1–D26), and Phase 2 of D:\aipithicus\aipithicus-issues\TeXdig\planning\roadmap.md.
Task in one paragraph: read the 25 Doccer source rows listed in the brief (Algebra shards Doccer_s001/s002 and Vectors shard s009 under D:\aipithicus\Doccer\src\Doccer.snapshot\20260831_103424) by byte range using the brief's PowerShell extraction recipe — via the pwsh_exec MCP or the PowerShell tool, not the Bash tool (it breaks on apostrophes) — in the brief's reading order, batching several rows per call. Then write D:\aipithicus\aipithicus-issues\TeXdig\planning\regions-design.md with the seven sections the brief specifies, mirroring substrate-design.md in shape and neutral register: (1) what each Doccer type establishes — invariant, validation posture, what it forgets on purpose, Doccer's stated verification baseline (cite D:\aipithicus\Doccer\docs\testing.md and docs\specification\contracts.md for the numbers); (2) TeXdig regions/ contracts — byte-based SpanSet and the byte ledger (every byte is a node or explicit residue), strict-stack Pairing with typed residue, three-valued (set/clear/unknown) masks over bytes, and whichever relation algebras survive the "does any Phase 2–4 consumer branch on it" test; (3) deferrals to structure/ and evidence/ with the consuming phase and a revival trigger for each; (4) conformance fixture law families with expectations stated per coordinate space (bytes for TeXdig, UTF-16 for Doccer, scalars for both), including the SpanSet bitmap-oracle method (≥200 random sets, adjacency, normalization); (5) a module map for packages/texdig/src/regions/; (6) decisions for the owner as proposals only — never rulings; (7) next steps. Honor the already-ruled substrate decisions (branded byte spans, non-empty occurrence batches, no SpanLevel, TeX-faithful line terminators, JSON conformance fixtures) and the code laws (zero dependencies, no enums under erasableSyntaxOnly, isolatedDeclarations, producer lineage on knowledge-bearing facts, evidence only where a consumer branches on it).
Guardrails (binding): design only — no code, no tests, no changes anywhere under D:\aipithicus\TeXdig. Do not edit substrate-design.md, decisions.md, roadmap.md, or deferred-register.md; put proposed amendments and proposed register entries in your report instead. No git commits anywhere. Record findings in the doc-dive journal under concept C-016: first call mdnav_discover with root D:\aipithicus\aipithicus-issues\TeXdig\ideation and run "latest", then mdnav_journal_record per reading step with anchors like { scope: "snap", unit: "s002 SpanSet.cs 52879..58742" } (the mdnav tools are deferred — load them with ToolSearch "select:mcp__mdnav__mdnav_discover,mcp__mdnav__mdnav_journal_record" first; if a record fails with "ref does not exist", re-run mdnav_discover and retry).
Finish by appending your completion report under the "## Report" heading of the brief file (use Edit on that heading): what was read, the shape of regions-design.md, the owner decisions it surfaces, proposed register entries (each with detail, trigger, evidence anchor), proposed amendments to the canon documents, and anything in the source that contradicted assumptions made in substrate-design.md.